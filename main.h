#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>
#include <unistd.h>
#include <stdarg.h>

void print_integer(int val);
int _putchar(char c);
int _fprintf(FILE *stream, const char *format, ...);
int _integer_length(int integer);

#endif
